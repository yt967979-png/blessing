# Reverse proxy for Blessing Power Guide on Lightsail/EC2.
# Replace hostnames with your domain (A record → static IP).
# Caddy obtains Let's Encrypt certificates automatically.
#
# Install: copy to /etc/caddy/Caddyfile then: sudo systemctl reload caddy

blessingpowerguide.in, www.blessingpowerguide.in {
	encode gzip
	reverse_proxy 127.0.0.1:3000
}

# Temporary smoke test without a domain (HTTP only — not for Google Sign-In):
# :80 {
# 	reverse_proxy 127.0.0.1:3000
# }
